High Priority fix:

- Second song (More Than Able) shows a “Key not found on plan item” message despite the key being selected (“C”)
- On the preview viewer, there are two major issues. The first is:
    - “Key of {X}:” does not behave as expected. Should include single letter (between A and G) info plus a possible accidental symbol (b for flat, # for sharp). Currently the program pulls the “subtitle” info.
        - Current behavior (example): 
            - Input: “C: Original Key”
            - Current Output: “Original Key”
        - Desired behavior (Example):
            - Input: “Ab: High Male”
            - Desired Output: “Ab”
- On the preview viewer, there are two major issues. The second is:
    - Songs that were not skipped list as “skipped.” It is unclear whether these are actually skipped or simply being mislabeled

Low priority fix:

- Service Opener Video lists as song item because of the given criteria. We need to adjust the criteria to only trigger for actual songs, or we need to make a meeting that default ignores certain items that may match search criteria
- Key signature should also be displayed onto the song’s preview window in the “songs“ section

---

## Fixes applied (2026-05-22)

| Feedback | Cause | Fix |
|----------|-------|-----|
| Wrong key in song list (`Original Key` vs `C`) | Used PCO Key `name` (arrangement label) | Use `starting_key` (+ minor), with fallbacks via `key_name` / `C: …` parsing (`src/lib/pco/format-key.ts`) |
| “Key not found” on More Than Able | Same + key not in first fetch | `include=song,key` on items; resolve from included Key, then item `key_name`, then item key endpoint |
| Preview shows “Skipped” for active songs | Preview treated missing `selectedFileId` as skipped | Separate `status`: skipped / no-selection / ready / error |
| Service Opener Video as song | Any item with song relationship matched | `item_type === "song"` only |
| Key on Songs step | Not rendered | Show **Key:** under song title in Songs step |

## Org Drive blank-scan fix (2026-05-22)

| Issue | Fix |
|-------|-----|
| Find blank scan not locating org files | Follow PCO URL only; `resolveDriveRoot` + subtree search; `corpora: allDrives`; shortcut resolution |
| Wrong file offered | Removed fallback that returned non-blank MASTER file when no `blank` match |
| Hard to debug | UI shows PCO link, search folder name, and explicit permission/empty-search errors |

## PCO arrangement URL vs Drive link (2026-05-22)

| Issue | Cause | Fix |
|-------|-------|-----|
| GREEN tier but PCO scan link is `services.planningcenteronline.com/.../arrangements/...` | PCO attachment `url` is the arrangement **page**, not Google Drive | Call PCO attachment **`open`** action → `attachment_url` (Drive); use plan item **arrangement** attachments |
