# Build feedback — 2026-05-22 (15:09)

## Failure point (apply phase)

**Symptom:** After “Approve & update Google Doc,” intro updated but song scans did not replace. Error on sign-off step:

```
Invalid requests[7].insertPageBreak: Index 3157 must be less than the end index of the referenced segment, 2993.
```

**Cause:** A single `batchUpdate` pre-computed insert indices for intro + multiple scan sections. Intro edits shortened the document, so later `insertPageBreak` indices were stale.

**Fix (2026-05-22):** `src/lib/docs/grg-mutate.ts` — intro in one batch; delete everything after intro; append **each** scan section in its **own** batch at the current document end (re-read doc length each time).

**Verify:** Full sign-off cycle — intro date + song list + all selected scan pages written; no `insertPageBreak` error.

---

## Other issues

| Issue | Fix |
|-------|-----|
| Skipped items (e.g. Service Opener Video) still in song list | Preview/apply use `songListForApply()` — excludes `skipped` flows (`page.tsx`). Service opener/video titles also filtered at PCO load (`isSongItem` in `plan-bundle.ts`). |
| No space after `Key of {X}:` | Song list lines: `Key of ${key}: ${title}` (`grg-mutate.ts` `buildSongListLines`). |
| Artist = song credits instead of arrangement | Artist from plan item **arrangement name** (`plan-bundle.ts` + `arrangement-display.ts`), not `song.author`. |
| Date reads `AMay 24th, 2026` | Date paragraph found via month/date regex; replace **inner** text only (`start+1` … `end-1`), not a partial run that left a stray `A`. |

**Deferred (not in this pass):** manual preview editing, pre-exclude list, two-column lyrics formatting.

---

## Follow-up (template + output copy)

Heuristic in-place edits replaced with **template copy** flow. See `docs/GRG-TEMPLATE.md`, `src/lib/docs/grg-template.ts`, and apply route (`recreateOutputFromTemplate` + placeholders). User must create template with `{{GRG_DATE}}`, `{{GRG_SONG_LIST}}`, `{{GRG_SCANS_BEGIN}}` and **Reconnect Google** after Drive write scope change.

---

## Original notes

Failure displayed on sign-off after incomplete apply — intro only, scans not replaced (see error above).

Skipped “Service Opener Video” still appeared in song list — easy fix chosen: omit skipped from apply list; core fix: narrower `isSongItem` + title filter.

Minor: spacing after key colon; arrangement-based artist; date leading `A`.
