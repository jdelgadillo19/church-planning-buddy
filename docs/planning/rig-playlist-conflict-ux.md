# Rig playlist conflict UX (deferred)

**Status:** Deferred — superseded by a planned **slide-deck UX workflow overhaul** (user workflow chart).

**Canonical plan (historical):** `.cursor/plans/rig_playlist_conflict_ux_26c2264b.plan.md`

## Summary

Incremental fix for Grapevine Rig playlist overwrite / keep-both / cancel was drafted after apply failures when a target playlist already exists. **Not implementing** until a broader workflow chart defines the full apply, conflict, and publish UX.

## Prerequisite

User-authored workflow chart for slide-deck + rig operator flows.

## Until then

Use existing rig apply path; overwrite conflicts may require manual ProPresenter cleanup or retry. Plan-format / disambiguation changes are separate.
