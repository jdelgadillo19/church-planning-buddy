On the confirmation page, the following listed in regards to the team name:

"
    Team roster (confirmed from PCO)
    AC Gui or Keys — no confirmed PCO match (template placeholder kept)
    Drums — no confirmed PCO match (template placeholder kept)
    Keys — no confirmed PCO match (template placeholder kept)
    WL — no confirmed PCO match (template placeholder kept)
    Part Lead — no confirmed PCO match (template placeholder kept)
"

I see what is happened, but it's more important to fix than understand.

reference document = "/Users/SBBWD/Projects/church-planning-buddy/downloads/Get Ready Guide (SUN).docx"

It appears that the program is using the reference tag from the reference GRG and looking for those positions specifically. This is incorrect. The program should instead read the confirmed positions on PCO and overwrite positions on the template. Source of truth for position lineup is confirmed planning center positions, not the reference document.

It may help to alter the reference document to replace the specified position tags with a placeholder. 
    In the reference doc, replace all position tags:
        "AC Gui or Keys"
        "Drums"
        "Keys"
        "WL"
       "Part Lead"
    with
        "[Position]"
    then delete all but one of the "[name]: [position]" except for one in each section. There should remain one "[name]: [position]" line in the BAND section and one in the CHOIR section

As currently implied in the reference document, the "Name" field should default alias to "[first name] [last initial]"


----------


Roster fails at read step. In final preview, roster outputs the following:

    "
    No confirmed Platform Team members (BAND / CHOIR / ALL TEAM) on this plan.
    "

Correct behavior should:
    Identify the in-scope teams ("Platform Team" in this case)
    Identify all individuals labeled "confirmed"
    Read the name of the person and the position
    Display name and position data in preview UI in proper format (first name, last initial; ": "; Aliased position title)
    After user initiates the "apply" command, write the details into the get ready guide in the BAND field and CHOIR field respectively, based on origin prefix
    In edge cases (i.e. "Guest"), prompt user to manually select BAND or CHOIR

:orrect output for 87788328 should be the following members (in no particular order for now) and write them into BAND or CHOIR section on the GRG:

BAND:   "Timothy K: Cajon"
        "Vivant R: MD"

CHOIR:  "Carey C: Room Singer"
        "Jesse D: WL"
        "Romina Y: Guest"

If there is a discrepancy between this test and output based on intended aliasing, prioritize predetermined aliasing.



----------



The information calling is working properly except for the duplicate issue. The same name should not appear twice in the same section, but can appear in both BAND and CHOIR sections simultaneously if the same person confirms positions in both. 
In cases where the same name appears multiple times under the same heading, allow the user to choose between two options:
    Select label to apply (manual tiebreak)
    Apply both tags to the same single line unit

For example:    if a band member returns as both MD and Keys player, user should manually be able to consolidate tags
                In current case, Carey C would only appear once in the "CHOIR" field with user selected (via checkbox) position tags
                In cases where adifferent member has multiple roles (i.e. Music Director && Drums), User can also select (via checkbox) BOTH labels to be applied

Also, we need to address format retention. I wonder if best practice for future modularity would be to use a reference document to retrieve formatting specs. For example:
    Meta: Filetype, Doc name, tags (such as date);
    Layout: New page, text alignment, margins, columns;
    Tagging: Titles, group labels (like team roster, song names), Song titles, Song credits, Song section labels (i.e. verse, chorus, bridge, etc.)
    Font: Typeface, Size, Bold/ital/etc. Caps, highlights; 
To use "Reference document" as an example for post-MVP goal behavior:
    Setup: Planning buddy would read this document, note the characteristics, then create a template that contains placeholders for all the existing elements in their proper formatting. This default template can be refined or reconfigured manually, or a new reference document can later be uploaded to either replace the existing template OR supplement a selectable template library.
    Execution: Based on current behavior, Planning Buddy would take the plaintext data retrieved from PCO, and copy it into the template without altering existing formatting
    Fail-cases: 
    
    Case: Someone manually edits the template and misspells or deletes a placeholder, causing there to be no reference format for Planning Buddy to paste into.
        If Planning Buddy has a piece of information, but doesn't have an existing formatting placeholder:
            MVP: Notify user of discrepancy. For now, allow user to choose cancel or skip
            Post MVP: Run automated test against reference copy (configurable) and altered template to infer user goal and generate suggested fixes
